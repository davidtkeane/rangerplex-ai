// Preload script for RangerChat Lite
// Note: contextIsolation is disabled for this app, so we don't use contextBridge

// This preload is minimal since the app uses WebSocket directly in the renderer
console.log('RangerChat Lite preload loaded')
